name: Build APK & Create Release

on:
  push:
    tags:
      - 'v*'
      - 'V*'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Tag name for release (e.g. v1.0.0)'
        required: false
        type: string
        default: ''
      release_title:
        description: 'Release title'
        required: false
        type: string
        default: ''

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build APK & Upload Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.3.1'
          validate-wrappers: false

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Ensure .env file exists
        run: |
          if [ -f .env.example ]; then
            cp .env.example .env
          else
            touch .env
          fi

      - name: Decode Keystore (Optional)
        env:
          ENCODED_KEY: ${{ secrets.KEYSTORE_BASE64 }}
        if: env.ENCODED_KEY != ''
        run: |
          echo "$ENCODED_KEY" | base64 -d > my-upload-key.jks

      - name: Build APKs with Gradle
        env:
          KEYSTORE_PATH: ${{ github.workspace }}/my-upload-key.jks
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          gradle assembleRelease assembleDebug --no-daemon

      - name: Determine Release Metadata
        id: release_info
        run: |
          TAG="${{ github.ref_name }}"
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.tag_name }}" ]; then
            TAG="${{ inputs.tag_name }}"
          fi
          
          TITLE="$TAG"
          if [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.release_title }}" ]; then
            TITLE="${{ inputs.release_title }}"
          fi

          echo "tag_name=${TAG}" >> $GITHUB_OUTPUT
          echo "release_title=${TITLE}" >> $GITHUB_OUTPUT

      - name: Prepare APK Release Assets
        run: |
          mkdir -p release-assets
          TAG_NAME="${{ steps.release_info.outputs.tag_name }}"
          
          if [ -f "app/build/outputs/apk/release/app-release.apk" ]; then
            cp app/build/outputs/apk/release/app-release.apk "release-assets/Percentify-${TAG_NAME}.apk"
            cp app/build/outputs/apk/release/app-release.apk "release-assets/Percentify-${TAG_NAME}-Release.apk"
          elif [ -f "app/build/outputs/apk/release/app-release-unsigned.apk" ]; then
            cp app/build/outputs/apk/release/app-release-unsigned.apk "release-assets/Percentify-${TAG_NAME}-Release-unsigned.apk"
          fi

          if [ -f "app/build/outputs/apk/debug/app-debug.apk" ]; then
            cp app/build/outputs/apk/debug/app-debug.apk "release-assets/Percentify-${TAG_NAME}.apk"
          fi

          echo "Prepared APKs in release-assets:"
          ls -la release-assets/

      - name: Upload APKs to Workflow Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Percentify-APKs-${{ steps.release_info.outputs.tag_name }}
          path: release-assets/*
          retention-days: 30

      - name: Create & Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          tag_name: ${{ steps.release_info.outputs.tag_name }}
          name: ${{ steps.release_info.outputs.release_title }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
